# contributing

## build

This project depends on openssl-sys, which depends on openssl. Ensure you have the necessary items
installed according to [its docs](https://docs.rs/openssl/latest/openssl/#automatic);
