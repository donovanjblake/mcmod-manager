# Modlist used by yours truly :)

[defaults]
game_version = "26.1.2"
loader = "fabric"

[projects]

# libraries (this section can be removed when dependency resolution is available)
# fabric-api.defaults = true
# cloth-config.defaults = true
# glitchcore.defaults = true

# graphics
complementary-reimagined = {defaults = true, loader = "iris"}
continuity.defaults = true
sodium.defaults = true
iris.defaults = true
distanthorizons.defaults = true
faithful-32x = { defaults = true, loader = "minecraft" }

# utility
lithium.defaults = true
modmenu.defaults = true
fpsdisplay.defaults = true

# generation
"dungeons+".defaults = true
dungeons-and-taverns.defaults = true
hopo-better-ruined-portals.defaults = true
hopo-better-underwater-ruins.defaults = true
stellarity.defaults = true

# other
blazeandcaves-advancements-pack = { defaults = true, loader = "datapack" }

[optional-projects]
biomes-o-plenty.defaults = true
camps_castles_carriages.defaults = true
scorched.defaults = true
tidal-towns.defaults = true
wabi-sabi-structures.defaults = true
